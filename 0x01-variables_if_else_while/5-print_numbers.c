#include <stdio.h>
#include <stdlib.h>
/* more headers goes there */

/**
 * main - prints results
 * Return: Returns 0
 */
int main(void)
{
	int n = 0;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
